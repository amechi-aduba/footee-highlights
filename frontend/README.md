# Footee Vision Frontend

## Run locally

```powershell
npm install
npm run dev
```

The app runs at `http://localhost:5173` and expects the backend at
`http://localhost:8000`. Override that URL with `VITE_API_BASE_URL` if needed.
